const nextConfig = {
  output: "export",
  images: {
    unoptimized: true,
  },
};

// module.exports = {
//   Target must be serverless
//   target: "serverless",
// };

module.exports = nextConfig;
